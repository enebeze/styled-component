import styled from 'styled-components/native'

export const TextSyled = styled.Text`
  color: ${(props) => props.theme.colors.secondary};
  font-size: 16px;
  line-height: 24px;
  font-style: normal;
`
